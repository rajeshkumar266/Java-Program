class Solution {
    int missingNumber(int arr[]) {
        int n=arr.length+1;
        int sum=0;
        n=n*(n+1)/2;
        for(int i=0;i<arr.length;i++){
            sum+=arr[i];
        }
        sum = n-sum;
        return sum;
        
    }
}
